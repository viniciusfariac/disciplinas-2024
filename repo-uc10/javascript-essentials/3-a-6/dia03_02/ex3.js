//Declare uma variável sem atribuir um valor a ela. Em seguida, verifique se ela é do tipo undefined usando uma estrutura condicional.
let variavel;
if(typeof variavel === 'undefined'){
    console.log("esta variavel é undefined")
}

